```react
import React, { useState, useEffect, useRef } from 'react';
import { 
  Sparkles, 
  Paintbrush, 
  Calculator, 
  MessageSquare, 
  Phone, 
  MapPin, 
  Layers, 
  Sun, 
  Moon, 
  ChevronRight, 
  Check, 
  Send, 
  ArrowRight, 
  Award, 
  Hammer, 
  Users, 
  Clock, 
  Calendar,
  X,
  Compass,
  ArrowUpRight
} from 'lucide-react';

// ==========================================
// CONFIGURATION & API SETUP
// ==========================================
const apiKey = ""; // Runtime automatically provisions this key
const MODEL_NAME = "gemini-2.5-flash-preview-09-2025";

// ==========================================
// HIGH-FIDELITY OFFICIAL LOGO COMPONENT (SVG)
// Recreates the exact uploaded brand asset
// ==========================================
function ShyamLogo({ className = "w-12 h-12", light = false }) {
  const brandBlue = "#2d5a7b"; // Exact slate-blue from logo
  const brandGrey = "#414143"; // Exact charcoal grey from logo
  const textColor = light ? "#1c1917" : "#ffffff";

  return (
    <svg 
      viewBox="0 0 240 220" 
      className={className} 
      fill="none" 
      xmlns="http://www.w3.org/2000/svg"
    >
      {/* 3 Charcoal Grey Bars */}
      <rect x="80" y="80" width="16" height="55" rx="2" fill={brandGrey} />
      <rect x="108" y="50" width="16" height="85" rx="2" fill={brandGrey} />
      <rect x="136" y="20" width="16" height="115" rx="2" fill={brandGrey} />

      {/* Blue Circular Frame with Gap for Arrow */}
      <path 
        d="M 172 135 A 72 72 0 1 1 180 60" 
        stroke={brandBlue} 
        strokeWidth="11" 
        strokeLinecap="round"
        fill="none"
      />

      {/* Climbing Zig-Zag Arrow */}
      <path 
        d="M 52 145 L 85 105 L 115 135 L 180 50" 
        stroke={brandBlue} 
        strokeWidth="11" 
        strokeLinecap="round" 
        strokeLinejoin="round"
      />
      {/* Arrow Head */}
      <path 
        d="M 148 48 L 184 46 L 182 82" 
        stroke={brandBlue} 
        strokeWidth="11" 
        strokeLinecap="round" 
        strokeLinejoin="round"
      />

      {/* Typography */}
      <text 
        x="120" 
        y="180" 
        textAnchor="middle" 
        fill={textColor} 
        fontSize="17" 
        fontWeight="bold" 
        letterSpacing="2"
        fontFamily="sans-serif"
      >
        SHYAM INTERIOR
      </text>
      <text 
        x="120" 
        y="200" 
        textAnchor="middle" 
        fill={light ? "#78716c" : "#a8a29e"} 
        fontSize="8" 
        fontWeight="600" 
        letterSpacing="2.5"
        fontFamily="sans-serif"
      >
        QUALITY IS OUR FIRST PRIORITY
      </text>
    </svg>
  );
}

export default function App() {
  // Theme & Global States
  const [theme, setTheme] = useState('dark'); // 'dark' (Obsidian & Gold) or 'light' (Ivory & Bronze)
  const [activeTab, setActiveTab] = useState('home');
  const [scrolled, setScrolled] = useState(false);

  // Cost Estimator State
  const [roomType, setRoomType] = useState('living');
  const [roomArea, setRoomArea] = useState(150); // sq ft
  const [materialGrade, setMaterialGrade] = useState('premium'); // standard, premium, ultra
  const [addons, setAddons] = useState({
    falseCeiling: true,
    modularKitchen: false,
    ambientLighting: true,
    wallpapers: false
  });

  // Visualizer State
  const [wallColor, setWallColor] = useState('#d4af37'); // Gold Accent
  const [floorType, setFloorType] = useState('marble'); // marble, wooden, tiles
  const [lightingMode, setLightingMode] = useState('warm'); // warm, cool, ambient

  // AI Assistant State
  const [aiPrompt, setAiPrompt] = useState('Design a modern living room with space-saving furniture');
  const [aiStyle, setAiStyle] = useState('Modern Minimalist');
  const [aiResponse, setAiResponse] = useState('');
  const [aiLoading, setAiLoading] = useState(false);
  const [aiError, setAiError] = useState('');

  // Booking & Inquiry Form State
  const [bookingForm, setBookingForm] = useState({
    name: '',
    phone: '',
    email: '',
    service: 'Residential Design',
    location: 'Andal',
    message: '',
    date: ''
  });
  const [formSubmitted, setFormSubmitted] = useState(false);

  // Gallery Filters
  const [galleryFilter, setGalleryFilter] = useState('all');

  // Handle Navbar Scroll effect
  useEffect(() => {
    const handleScroll = () => {
      if (window.scrollY > 50) {
        setScrolled(true);
      } else {
        setScrolled(false);
      }
    };
    window.addEventListener('scroll', handleScroll);
    return () => window.removeEventListener('scroll', handleScroll);
  }, []);

  // Theme Toggler
  const toggleTheme = () => {
    setTheme(prev => prev === 'dark' ? 'light' : 'dark');
  };

  // ==========================================
  // REAL-TIME BUDGET ESTIMATOR LOGIC
  // ==========================================
  const calculateCost = () => {
    let baseRate = 120; // standard base rate per sq ft
    if (materialGrade === 'premium') baseRate = 220;
    if (materialGrade === 'ultra') baseRate = 380;

    if (roomType === 'kitchen') baseRate += 150; // Kitchen fittings markup
    if (roomType === 'bedroom') baseRate += 40;

    let total = roomArea * baseRate;

    // Addon rates
    if (addons.falseCeiling) total += roomArea * 90; // Rs 90/sqft for false ceiling
    if (addons.modularKitchen && roomType === 'kitchen') total += 120000; // Flat estimate for modular cabinets
    if (addons.ambientLighting) total += 15000;
    if (addons.wallpapers) total += 25000;

    return {
      baseCost: Math.round(roomArea * baseRate),
      addonsCost: Math.round(
        (addons.falseCeiling ? roomArea * 90 : 0) +
        (addons.modularKitchen && roomType === 'kitchen' ? 120000 : 0) +
        (addons.ambientLighting ? 15000 : 0) +
        (addons.wallpapers ? 25000 : 0)
      ),
      total: Math.round(total)
    };
  };

  const costResult = calculateCost();

  // ==========================================
  // GEMINI AI INTEGRATION (EXPONENTIAL BACKOFF)
  // ==========================================
  const generateAIDesign = async () => {
    if (!aiPrompt.trim()) return;
    setAiLoading(true);
    setAiError('');
    setAiResponse('');

    const systemPrompt = `You are a world-class premium interior designer working exclusively for "Shyam Interior" located in Andal, West Bengal. 
    Provide highly personalized, luxurious, yet culturally relevant interior advice for homes in the Durgapur-Asansol-Andal region. 
    Use rich tone, precise structural steps, material recommendations suited for West Bengal climate (humidity-resistant PVC, premium Saint-Gobain Gypsum, moisture-proof BWR plywood), and include a recommended color palette. 
    Structure your answer neatly with bullet points and bold titles. Keep it elegant, clear, and action-oriented.`;

    const userQuery = `Help me design a room with these specifications:
    - Style Chosen: ${aiStyle}
    - Specific Request/Dimensions: ${aiPrompt}
    - Location: Andal/Durgapur, WB
    Provide a luxurious concept breakdown including:
    1. Premium layout Concept
    2. False Ceiling & Lighting Recommendations (Gypsum, LED Warm Coves)
    3. Suggested Colors & Wall Treatments
    4. Ideal Materials for Andal's climate`;

    let attempt = 0;
    const maxRetries = 5;
    let success = false;

    while (attempt < maxRetries && !success) {
      try {
        const response = await fetch(
          `https://generativelanguage.googleapis.com/v1beta/models/${MODEL_NAME}:generateContent?key=${apiKey}`,
          {
            method: 'POST',
            headers: {
              'Content-Type': 'application/json'
            },
            body: JSON.stringify({
              contents: [{ parts: [{ text: userQuery }] }],
              systemInstruction: { parts: [{ text: systemPrompt }] }
            })
          }
        );

        if (!response.ok) {
          throw new Error(`API returned status ${response.status}`);
        }

        const data = await response.json();
        const text = data.candidates?.[0]?.content?.parts?.[0]?.text;
        if (text) {
          // Format text markdown slightly for HTML display
          const formattedText = text
            .replace(/\n/g, '<br />')
            .replace(/\*\*(.*?)\*\*/g, '<strong>$1</strong>')
            .replace(/\*(.*?)\*/g, '<em>$1</em>');
          setAiResponse(formattedText);
          success = true;
        } else {
          throw new Error("Empty response payload");
        }
      } catch (err) {
        attempt++;
        if (attempt >= maxRetries) {
          setAiError("Our designer AI is currently in heavy demand. Please try again in a few moments, or call us directly!");
        } else {
          // Exponential backoff delay
          await new Promise(resolve => setTimeout(resolve, Math.pow(2, attempt) * 1000));
        }
      }
    }
    setAiLoading(false);
  };

  // Form submit handler
  const handleFormSubmit = (e) => {
    e.preventDefault();
    setFormSubmitted(true);
    setTimeout(() => {
      setFormSubmitted(false);
      setBookingForm({
        name: '',
        phone: '',
        email: '',
        service: 'Residential Design',
        location: 'Andal',
        message: '',
        date: ''
      });
    }, 5000);
  };

  // Static Data for Showcase & Services
  const services = [
    {
      title: "Premium Residential Design",
      desc: "Turnkey luxury homes crafted with exquisite materials, bespoke wardrobes, and modern layouts optimized for comfort.",
      icon: Paintbrush,
      accent: "from-amber-500 to-yellow-600"
    },
    {
      title: "High-End False Ceilings",
      desc: "Architectural false ceilings using finest Gypsum, PVC, or wooden panels integrated with smart ambient lighting controls.",
      icon: Layers,
      accent: "from-yellow-600 to-amber-700"
    },
    {
      title: "Modular Smart Kitchens",
      desc: "Ergonomic, water-resistant, chimney-equipped sleek kitchens with soft-close hardware and pull-out organizers.",
      icon: Compass,
      accent: "from-amber-600 to-orange-600"
    },
    {
      title: "Commercial & Office Spaces",
      desc: "High-productivity work environments, boutique hotels, retail showrooms designed to inspire clients and teams alike.",
      icon: Award,
      accent: "from-yellow-500 to-amber-500"
    }
  ];

  const galleryItems = [
    { title: "Royal Living Room", category: "living", location: "Kajora, Andal", imageColor: "bg-gradient-to-tr from-stone-900 to-stone-700", tags: ["False Ceiling", "CNC Jali", "Italian Marble"] },
    { title: "Elite Modular Kitchen", category: "kitchen", location: "Andal Airport Area", imageColor: "bg-gradient-to-tr from-amber-950 to-stone-800", tags: ["Acrylic", "Soft Close", "LED Profile"] },
    { title: "Elegant Master Bedroom", category: "bedroom", location: "Ukhra Road, Andal", imageColor: "bg-gradient-to-tr from-stone-850 to-amber-900", tags: ["Upholstered Wall", "WPC Panels"] },
    { title: "Modern PVC Ceiling Concept", category: "ceiling", location: "Raniganj bypass", imageColor: "bg-gradient-to-tr from-neutral-900 to-amber-950", tags: ["PVC Panels", "Spotlights"] },
    { title: "Luxurious Dining Lounge", category: "living", location: "Durgapur Link Rd", imageColor: "bg-gradient-to-tr from-amber-900 to-stone-950", tags: ["Chandelier", "Warm Wooden Accents"] },
    { title: "Sleek Office Lobby", category: "office", location: "Andal Junction Plaza", imageColor: "bg-gradient-to-tr from-zinc-900 to-amber-900", tags: ["Acoustic Gypsum", "Minimalist Workstations"] }
  ];

  // Helper styles for Theme
  const isDark = theme === 'dark';
  const bgClass = isDark ? 'bg-[#0f0e0c] text-neutral-100' : 'bg-[#faf9f6] text-stone-900';
  const cardBgClass = isDark ? 'bg-[#181613] border-neutral-800' : 'bg-white border-stone-200';
  const textMutedClass = isDark ? 'text-neutral-400' : 'text-stone-600';
  const accentGold = isDark ? 'text-[#d4af37]' : 'text-[#b8860b]';
  const borderAccent = isDark ? 'border-[#d4af37]/30' : 'border-[#b8860b]/30';
  const buttonGold = isDark 
    ? 'bg-gradient-to-r from-[#d4af37] to-[#f3e5ab] text-neutral-950 hover:brightness-110' 
    : 'bg-gradient-to-r from-[#b8860b] to-[#d4af37] text-white hover:brightness-105';

  return (
    <div className={`min-h-screen font-sans transition-colors duration-300 ${bgClass}`}>
      
      {/* ==========================================
          NAVBAR
          ========================================== */}
      <nav className={`fixed top-0 left-0 w-full z-50 transition-all duration-300 ${
        scrolled 
          ? isDark ? 'bg-[#0f0e0c]/95 backdrop-blur-md shadow-2xl border-b border-neutral-800/60' : 'bg-[#faf9f6]/95 backdrop-blur-md shadow-md border-b border-stone-200/60'
          : 'bg-transparent'
      }`}>
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div className="flex items-center justify-between h-20">
            {/* Inline Vector Brand Logo */}
            <div className="flex items-center gap-3 cursor-pointer" onClick={() => setActiveTab('home')}>
              <div className="w-12 h-12 flex items-center justify-center bg-white/5 rounded-xl border border-white/10 p-1">
                <ShyamLogo className="w-10 h-10" light={!isDark} />
              </div>
              <div>
                <span className="text-xl font-bold tracking-widest block uppercase">
                  SHYAM <span className={accentGold}>INTERIOR</span>
                </span>
                <span className="text-[10px] tracking-wider uppercase font-semibold block opacity-80 -mt-1">
                  Andal's Luxury Standard
                </span>
              </div>
            </div>

            {/* Nav Tabs */}
            <div className="hidden md:flex items-center space-x-8">
              {[
                { id: 'home', label: 'Home' },
                { id: 'visualizer', label: 'Interactive Visualizer' },
                { id: 'ai-designer', label: 'AI Design Helper' },
                { id: 'estimator', label: 'Cost Estimator' },
                { id: 'portfolio', label: 'Portfolio' },
                { id: 'contact', label: 'Get In Touch' }
              ].map((tab) => (
                <button
                  key={tab.id}
                  onClick={() => setActiveTab(tab.id)}
                  className={`text-sm font-semibold tracking-wide transition-all uppercase relative py-2 ${
                    activeTab === tab.id 
                      ? `${accentGold} after:absolute after:bottom-0 after:left-0 after:w-full after:h-[2px] after:bg-[#d4af37]` 
                      : 'hover:text-amber-500 text-opacity-80'
                  }`}
                >
                  {tab.label}
                </button>
              ))}
            </div>

            {/* Theme & CTA Button */}
            <div className="flex items-center gap-4">
              <button 
                onClick={toggleTheme} 
                className={`p-2.5 rounded-full transition-all duration-300 ${
                  isDark ? 'bg-neutral-800 text-yellow-400 hover:bg-neutral-700' : 'bg-stone-200 text-stone-800 hover:bg-stone-300'
                }`}
                aria-label="Toggle Theme"
              >
                {isDark ? <Sun className="w-5 h-5" /> : <Moon className="w-5 h-5" />}
              </button>
              <button
                onClick={() => setActiveTab('contact')}
                className={`hidden lg:flex items-center gap-2 text-xs font-bold tracking-wider uppercase px-5 py-3 rounded-full transition-all ${buttonGold} shadow-md`}
              >
                Book Consultation <ArrowUpRight className="w-4 h-4" />
              </button>
            </div>
          </div>
        </div>
      </nav>

      {/* Mobile Nav Bar Drawer */}
      <div className="md:hidden fixed bottom-0 left-0 w-full z-50 bg-[#14120f]/95 backdrop-blur-md border-t border-neutral-800 py-3 px-2 flex justify-around items-center">
        {[
          { id: 'home', label: 'Home', icon: Paintbrush },
          { id: 'visualizer', label: 'Visualizer', icon: Layers },
          { id: 'ai-designer', label: 'AI Help', icon: Sparkles },
          { id: 'estimator', label: 'Estimate', icon: Calculator },
          { id: 'contact', label: 'Contact', icon: Phone }
        ].map((tab) => {
          const Icon = tab.icon;
          const isSelected = activeTab === tab.id;
          return (
            <button
              key={tab.id}
              onClick={() => setActiveTab(tab.id)}
              className={`flex flex-col items-center gap-1 flex-1 transition-all ${
                isSelected ? 'text-[#d4af37]' : 'text-neutral-400'
              }`}
            >
              <Icon className="w-5 h-5" />
              <span className="text-[10px] font-medium tracking-tight">{tab.label}</span>
            </button>
          );
        })}
      </div>

      {/* Spacer for Fixed Nav */}
      <div className="h-20"></div>

      {/* ==========================================
          TAB 1: HOME PAGE HERO
          ========================================== */}
      {activeTab === 'home' && (
        <div>
          {/* Hero Section with Official Logo Watermark */}
          <section className="relative overflow-hidden py-16 lg:py-28">
            
            {/* STUNNING WATERMARK LOGO HERO BACKDROP */}
            <div className="absolute inset-0 flex items-center justify-center opacity-[0.06] md:opacity-[0.09] pointer-events-none z-0">
              <ShyamLogo className="w-[320px] h-[320px] md:w-[480px] md:h-[480px]" light={!isDark} />
            </div>

            {/* Additional Decorative Ambient Backgrounds */}
            <div className="absolute inset-0 opacity-10 pointer-events-none z-0">
              <div className="absolute top-1/4 left-1/4 w-96 h-96 rounded-full bg-yellow-500/20 blur-3xl"></div>
              <div className="absolute bottom-1/3 right-1/4 w-96 h-96 rounded-full bg-amber-700/20 blur-3xl"></div>
            </div>

            <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
              <div className="grid grid-cols-1 lg:grid-cols-12 gap-12 items-center">
                
                {/* Hero Info */}
                <div className="lg:col-span-7 space-y-6">
                  <div className={`inline-flex items-center gap-2 px-4 py-1.5 rounded-full text-xs font-semibold uppercase tracking-wider ${
                    isDark ? 'bg-amber-500/10 text-amber-400 border border-amber-500/20' : 'bg-amber-100 text-amber-800'
                  }`}>
                    <Sparkles className="w-4 h-4" /> Quality is our first priority
                  </div>
                  
                  <h1 className="text-4xl sm:text-6xl font-black tracking-tight leading-[1.1]">
                    Crafting Elite <br />
                    <span className={`bg-clip-text text-transparent bg-gradient-to-r ${isDark ? 'from-[#d4af37] via-[#f3e5ab] to-[#e6ca65]' : 'from-[#8a640f] to-[#d4af37]'}`}>
                      Interiors in Andal
                    </span>
                  </h1>
                  
                  <p className={`text-lg sm:text-xl leading-relaxed max-w-xl ${textMutedClass}`}>
                    Welcome to the digital atelier of <strong className={accentGold}>Shyam Interior</strong>. 
                    From premium Gypsum False Ceilings to ultra-modern Modular Kitchens and luxurious spatial architectures, we build timeless environments in Andal and surrounding areas.
                  </p>

                  <div className="flex flex-col sm:flex-row gap-4 pt-4">
                    <button
                      onClick={() => setActiveTab('visualizer')}
                      className={`px-8 py-4 rounded-xl text-sm font-bold tracking-wider uppercase transition-all shadow-xl flex items-center justify-center gap-2 ${buttonGold}`}
                    >
                      Launch Studio Visualizer <Paintbrush className="w-4 h-4" />
                    </button>
                    <button
                      onClick={() => setActiveTab('estimator')}
                      className={`px-8 py-4 rounded-xl text-sm font-bold tracking-wider uppercase border-2 transition-all flex items-center justify-center gap-2 ${
                        isDark ? 'border-neutral-700 text-white hover:bg-neutral-800' : 'border-stone-300 text-stone-800 hover:bg-stone-100'
                      }`}
                    >
                      Calculate Cost <Calculator className="w-4 h-4" />
                    </button>
                  </div>

                  {/* Highlights Grid */}
                  <div className="grid grid-cols-3 gap-6 pt-10 border-t border-neutral-800/10">
                    <div>
                      <h4 className="text-3xl font-extrabold text-amber-500">12+</h4>
                      <p className={`text-xs uppercase tracking-widest font-semibold ${textMutedClass}`}>Years Legacy</p>
                    </div>
                    <div>
                      <h4 className="text-3xl font-extrabold text-amber-500">450+</h4>
                      <p className={`text-xs uppercase tracking-widest font-semibold ${textMutedClass}`}>Spaces Styled</p>
                    </div>
                    <div>
                      <h4 className="text-3xl font-extrabold text-amber-500">100%</h4>
                      <p className={`text-xs uppercase tracking-widest font-semibold ${textMutedClass}`}>Quality Rated</p>
                    </div>
                  </div>

                </div>

                {/* Hero Interactive Showcase / Graphic Card */}
                <div className="lg:col-span-5 relative">
                  <div className={`p-8 rounded-3xl border ${cardBgClass} shadow-2xl relative overflow-hidden group`}>
                    
                    {/* Simulated Premium Room Mockup */}
                    <div className="relative aspect-[4/3] rounded-2xl overflow-hidden bg-neutral-900 shadow-inner flex items-center justify-center">
                      {/* Dynamic Background simulating live rendering */}
                      <div className="absolute inset-0 bg-cover bg-center transition-all duration-700 filter brightness-90" 
                           style={{ 
                             backgroundColor: wallColor,
                             backgroundImage: 'radial-gradient(circle at top right, rgba(255,255,255,0.15), rgba(0,0,0,0.5))' 
                           }}>
                      </div>
                      
                      {/* Simulated Wireframe Overlay */}
                      <div className="absolute inset-0 border border-white/10 flex flex-col justify-between p-4">
                        <div className="flex justify-between items-start">
                          <span className="text-[10px] font-mono px-2 py-1 bg-black/60 rounded text-amber-400 backdrop-blur-sm">
                            RENDERER v2.5
                          </span>
                          <span className="text-[10px] font-mono px-2 py-1 bg-black/60 rounded text-green-400 backdrop-blur-sm">
                            ACTIVE
                          </span>
                        </div>
                        
                        {/* Center Graphic */}
                        <div className="w-full text-center space-y-1 bg-black/70 py-3 rounded-lg backdrop-blur-md">
                          <p className="text-xs font-bold text-white uppercase tracking-wider">Interactive Live Design Lab</p>
                          <p className="text-[10px] text-neutral-300">Click below to change styles real-time</p>
                        </div>
                      </div>

                      {/* Visual room items (SVG styled ceiling and lights) */}
                      <svg viewBox="0 0 400 300" className="absolute inset-0 w-full h-full pointer-events-none opacity-80">
                        {/* False Ceiling Profile */}
                        <path d="M 40 40 L 360 40 L 320 80 L 80 80 Z" fill="rgba(255,255,255,0.07)" stroke="rgba(255,255,255,0.2)" strokeWidth="1" />
                        <rect x="120" y="55" width="160" height="15" rx="2" fill="none" stroke="#ffeaa7" strokeWidth="2" className="animate-pulse" />
                        
                        {/* Spotlights */}
                        <circle cx="90" cy="50" r="4" fill="#fff" />
                        <circle cx="310" cy="50" r="4" fill="#fff" />
                        {/* Dynamic lighting glow */}
                        <polygon points="90,50 40,180 140,180" fill="url(#lightGlow)" opacity="0.3" />
                        <polygon points="310,50 260,180 360,180" fill="url(#lightGlow)" opacity="0.3" />
                        
                        <defs>
                          <linearGradient id="lightGlow" x1="0" y1="0" x2="0" y2="1">
                            <stop offset="0%" stopColor="#fff" stopOpacity="0.4" />
                            <stop offset="100%" stopColor="#d4af37" stopOpacity="0" />
                          </linearGradient>
                        </defs>
                      </svg>
                    </div>

                    <div className="mt-6 space-y-4">
                      <div className="flex justify-between items-center">
                        <h3 className="font-bold text-lg">Instant Customizer</h3>
                        <span className="text-xs font-semibold text-amber-500">Try Accent Paints:</span>
                      </div>
                      <div className="flex gap-3 justify-center">
                        {[
                          { color: '#d4af37', name: 'Imperial Gold' },
                          { color: '#1c3144', name: 'Deep Sapphire' },
                          { color: '#3f5e4d', name: 'Royal Emerald' },
                          { color: '#8b0000', name: 'Terracotta Red' },
                          { color: '#2c2c2c', name: 'Charcoal Silk' }
                        ].map((item, idx) => (
                          <button
                            key={idx}
                            onClick={() => setWallColor(item.color)}
                            style={{ backgroundColor: item.color }}
                            className={`w-10 h-10 rounded-full border-2 transition-all transform hover:scale-110 ${
                              wallColor === item.color ? 'border-white scale-110 shadow-lg' : 'border-transparent'
                            }`}
                            title={item.name}
                          />
                        ))}
                      </div>
                      <div className="text-center pt-2">
                        <button
                          onClick={() => setActiveTab('visualizer')}
                          className="text-xs font-bold text-amber-500 hover:underline flex items-center justify-center gap-1 mx-auto"
                        >
                          Explore Full Interactive Studio <ChevronRight className="w-4 h-4" />
                        </button>
                      </div>
                    </div>

                  </div>
                </div>

              </div>
            </div>
          </section>

          {/* ==========================================
              WHY CHOOSE US & CORE VALUES
              ========================================== */}
          <section className={`py-16 ${isDark ? 'bg-[#12110e]' : 'bg-stone-50'}`}>
            <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
              <div className="text-center max-w-3xl mx-auto space-y-4 mb-16">
                <h2 className="text-xs uppercase tracking-widest font-extrabold text-amber-500">Excellence in Execution</h2>
                <h3 className="text-3xl sm:text-4xl font-black">Why Shyam Interior is Andal's Preferred Choice</h3>
                <p className={textMutedClass}>
                  We bridge the gap between architectural perfectionism and everyday functional comfort. Here is why homeowners and businesses trust us completely.
                </p>
              </div>

              <div className="grid grid-cols-1 md:grid-cols-3 gap-8">
                
                <div className={`p-8 rounded-2xl border ${cardBgClass} space-y-4 transition-all hover:-translate-y-1 shadow-md`}>
                  <div className={`w-12 h-12 rounded-xl flex items-center justify-center ${isDark ? 'bg-amber-500/10 text-amber-400' : 'bg-amber-100 text-amber-800'}`}>
                    <Award className="w-6 h-6" />
                  </div>
                  <h4 className="text-xl font-bold">Premium Materials Only</h4>
                  <p className={`text-sm leading-relaxed ${textMutedClass}`}>
                    We source best-in-class BWR (Boiling Water Resistant) plywood, Saint-Gobain plasterboards, and heavy-gauge rust-free PVC ceiling frameworks suited for Andal's climate conditions.
                  </p>
                </div>

                <div className={`p-8 rounded-2xl border ${cardBgClass} space-y-4 transition-all hover:-translate-y-1 shadow-md`}>
                  <div className={`w-12 h-12 rounded-xl flex items-center justify-center ${isDark ? 'bg-amber-500/10 text-amber-400' : 'bg-amber-100 text-amber-800'}`}>
                    <Clock className="w-6 h-6" />
                  </div>
                  <h4 className="text-xl font-bold">On-Time Turnkey Delivery</h4>
                  <p className={`text-sm leading-relaxed ${textMutedClass}`}>
                    We respect your milestones. Our strictly managed site timelines guarantee that we hand over keys to your dream workspace or home precisely on the agreed date with zero quality compromises.
                  </p>
                </div>

                <div className={`p-8 rounded-2xl border ${cardBgClass} space-y-4 transition-all hover:-translate-y-1 shadow-md`}>
                  <div className={`w-12 h-12 rounded-xl flex items-center justify-center ${isDark ? 'bg-amber-500/10 text-amber-400' : 'bg-amber-100 text-amber-800'}`}>
                    <Users className="w-6 h-6" />
                  </div>
                  <h4 className="text-xl font-bold">Local Expertise, Global Standards</h4>
                  <p className={`text-sm leading-relaxed ${textMutedClass}`}>
                    Based locally in Andal/Asansol area, we provide responsive support, prompt on-site adjustments, and custom craftsmanship while delivering modern designs inspired by global trends.
                  </p>
                </div>

              </div>
            </div>
          </section>

          {/* ==========================================
              SERVICES GRID SHOWCASE
              ========================================== */}
          <section className="py-20 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div className="flex flex-col md:flex-row justify-between items-start md:items-end mb-12 gap-4">
              <div>
                <h2 className="text-xs uppercase tracking-widest font-extrabold text-amber-500">Our Offerings</h2>
                <h3 className="text-3xl sm:text-4xl font-black mt-2">Tailored Interior Services</h3>
              </div>
              <p className={`max-w-md ${textMutedClass}`}>
                Whether you need a complete design consultation, modular kitchen execution, or high-quality architectural false ceilings.
              </p>
            </div>

            <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
              {services.map((svc, i) => {
                const IconComp = svc.icon;
                return (
                  <div 
                    key={i} 
                    className={`p-6 rounded-2xl border ${cardBgClass} relative overflow-hidden group transition-all hover:border-amber-500/50 shadow-md flex flex-col justify-between h-[300px]`}
                  >
                    {/* Glow effect on hover */}
                    <div className="absolute top-0 right-0 w-32 h-32 bg-amber-500/5 rounded-full blur-2xl group-hover:bg-amber-500/20 transition-all"></div>
                    
                    <div className="space-y-4">
                      <div className={`w-10 h-10 rounded-lg flex items-center justify-center bg-gradient-to-tr ${svc.accent} text-white shadow-md`}>
                        <IconComp className="w-5 h-5" />
                      </div>
                      <h4 className="text-lg font-bold group-hover:text-amber-500 transition-colors">{svc.title}</h4>
                      <p className={`text-xs leading-relaxed ${textMutedClass}`}>{svc.desc}</p>
                    </div>

                    <button 
                      onClick={() => {
                        setBookingForm(prev => ({ ...prev, service: svc.title }));
                        setActiveTab('contact');
                      }}
                      className="text-xs font-bold text-amber-500 hover:underline flex items-center gap-1 pt-4 border-t border-neutral-800/10"
                    >
                      Enquire For Service <ArrowRight className="w-3.5 h-3.5" />
                    </button>
                  </div>
                );
              })}
            </div>
          </section>

          {/* ==========================================
              INTERACTIVE COST CALCULATOR BANNER
              ========================================== */}
          <section className={`py-16 ${isDark ? 'bg-gradient-to-b from-[#12110e] to-[#0f0e0c]' : 'bg-gradient-to-b from-stone-100 to-white'}`}>
            <div className="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8">
              <div className={`p-8 md:p-12 rounded-3xl border ${borderAccent} bg-gradient-to-tr ${isDark ? 'from-[#1b1915] via-[#12110e] to-[#25221b]' : 'from-stone-100 to-amber-50/50'} shadow-2xl relative overflow-hidden`}>
                <div className="absolute top-0 right-0 w-64 h-64 bg-[#d4af37]/10 rounded-full blur-3xl"></div>
                <div className="grid grid-cols-1 md:grid-cols-2 gap-8 items-center relative z-10">
                  <div className="space-y-4">
                    <span className="text-xs uppercase tracking-wider font-bold text-amber-500">Interactive Estimator</span>
                    <h3 className="text-2xl sm:text-3xl font-black">Want to plan your interior budget?</h3>
                    <p className={`text-sm leading-relaxed ${textMutedClass}`}>
                      We have designed a smart calculator factoring local Andal costs of premium raw wood, labor, high-durability hardware, and designer ceiling charges.
                    </p>
                    <button
                      onClick={() => setActiveTab('estimator')}
                      className={`px-6 py-3 rounded-xl text-xs font-bold tracking-wider uppercase transition-all shadow-md flex items-center gap-2 ${buttonGold}`}
                    >
                      Open Live Calculator <Calculator className="w-4 h-4" />
                    </button>
                  </div>
                  <div className="bg-black/40 p-6 rounded-2xl border border-white/5 backdrop-blur-md space-y-4 text-center">
                    <h4 className="text-xs uppercase tracking-wider font-bold text-amber-500">Average False Ceiling Rate</h4>
                    <p className="text-4xl font-extrabold text-white">₹90 <span className="text-xs text-neutral-400 font-normal">/ sq.ft.</span></p>
                    <p className="text-xs text-neutral-400">Includes expert architectural layout, sturdy heavy frame, high-grade Saint-Gobain board & flawless primer finish.</p>
                  </div>
                </div>
              </div>
            </div>
          </section>
        </div>
      )}

      {/* ==========================================
          TAB 2: INTERACTIVE VISUALIZER
          ========================================== */}
      {activeTab === 'visualizer' && (
        <section className="py-12 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div className="text-center max-w-2xl mx-auto mb-10 space-y-3">
            <h2 className="text-xs uppercase tracking-widest font-extrabold text-amber-500">Studio Playground</h2>
            <h3 className="text-3xl font-black">Interactive Style Visualizer</h3>
            <p className={textMutedClass}>
              Experiment with room colors, floor finishes, and ambient lighting to see how different configurations completely alter the energy of your space.
            </p>
          </div>

          <div className="grid grid-cols-1 lg:grid-cols-12 gap-8 items-stretch">
            
            {/* Live Canvas Visualizer Panel */}
            <div className="lg:col-span-8 flex flex-col justify-between">
              <div className={`p-6 rounded-3xl border ${cardBgClass} flex-1 flex flex-col justify-between shadow-2xl relative overflow-hidden`}>
                
                {/* Visualizer Frame */}
                <div className="relative aspect-[16/9] rounded-2xl overflow-hidden shadow-inner flex items-center justify-center bg-zinc-900 border border-neutral-800">
                  
                  {/* WALL BACKGROUND - Color reactive */}
                  <div className="absolute inset-0 transition-colors duration-500" style={{ backgroundColor: wallColor }}></div>

                  {/* ROOM SHADING & DEPTH PATTERN Overlay */}
                  <div className="absolute inset-0 bg-gradient-to-b from-black/40 via-transparent to-black/70 pointer-events-none"></div>

                  {/* DYNAMIC FLOOR RENDERER */}
                  <div className="absolute bottom-0 left-0 w-full h-[35%] transition-all duration-300" style={{
                    background: floorType === 'marble' 
                      ? 'linear-gradient(to top, rgba(240, 240, 240, 0.9), rgba(200, 200, 200, 0.5))' 
                      : floorType === 'wooden'
                        ? 'linear-gradient(to top, #8b5a2b, #5c3818)'
                        : 'linear-gradient(to top, #d3d3d3, #a9a9a9)',
                    backgroundImage: floorType === 'marble' 
                      ? 'repeating-linear-gradient(45deg, rgba(0,0,0,0.03) 0px, rgba(0,0,0,0.03) 10px, transparent 10px, transparent 20px)' 
                      : floorType === 'wooden'
                        ? 'repeating-linear-gradient(0deg, rgba(0,0,0,0.15) 0px, rgba(0,0,0,0.15) 20px, rgba(255,255,255,0.05) 20px, rgba(255,255,255,0.05) 40px)'
                        : 'repeating-linear-gradient(90deg, rgba(0,0,0,0.05) 0px, rgba(0,0,0,0.05) 50px, transparent 50px, transparent 100px)'
                  }}>
                    {/* Floor Reflection overlay */}
                    <div className="w-full h-full bg-gradient-to-t from-transparent via-white/5 to-white/20"></div>
                  </div>

                  {/* DYNAMIC LIGHTING RAY EFFECTS */}
                  <div className={`absolute inset-0 transition-opacity duration-500 pointer-events-none ${
                    lightingMode === 'warm' ? 'bg-amber-400/10' : lightingMode === 'cool' ? 'bg-sky-400/5' : 'bg-purple-500/5'
                  }`}></div>

                  {/* SVG Isometric Outline of the Living Space (Furniture, Ceiling & Plants) */}
                  <svg viewBox="0 0 800 450" className="absolute inset-0 w-full h-full pointer-events-none z-10">
                    
                    {/* False Ceiling Layout */}
                    <polygon points="100,20 700,20 620,100 180,100" fill="rgba(255,255,255,0.08)" stroke="rgba(255,255,255,0.2)" strokeWidth="1.5" />
                    
                    {/* Recessed LED Cove Lighting */}
                    <polygon points="120,30 680,30 610,90 190,90" fill="none" stroke={lightingMode === 'warm' ? '#fbbf24' : lightingMode === 'cool' ? '#38bdf8' : '#e879f9'} strokeWidth="4" opacity="0.8" className="animate-pulse" />
                    
                    {/* Hanging Modern Chandelier / Light Bulbs */}
                    <line x1="400" y1="20" x2="400" y2="120" stroke="#777" strokeWidth="2" />
                    {/* Outer frame of chandelier */}
                    <circle cx="400" cy="120" r="15" fill="none" stroke="#d4af37" strokeWidth="2" />
                    <circle cx="400" cy="120" r="5" fill={lightingMode === 'warm' ? '#fbbf24' : '#fff'} className="animate-ping" />

                    {/* Left & Right Decorative Wall Panels */}
                    <polygon points="40,110 160,110 160,350 40,350" fill="rgba(0,0,0,0.15)" stroke="rgba(255,255,255,0.1)" />
                    <line x1="100" y1="110" x2="100" y2="350" stroke="rgba(255,255,255,0.2)" strokeWidth="1" />
                    
                    {/* Modern Sofa */}
                    <rect x="250" y="240" width="300" height="90" rx="15" fill="#1e1e24" opacity="0.95" stroke="#333" />
                    <rect x="270" y="220" width="260" height="40" rx="10" fill="#2d2d35" />
                    {/* Cushions */}
                    <rect x="290" y="250" width="90" height="50" rx="5" fill={wallColor} opacity="0.7" />
                    <rect x="420" y="250" width="90" height="50" rx="5" fill="#e2e8f0" opacity="0.3" />

                    {/* TV Stand / Console */}
                    <rect x="200" y="160" width="10" height="150" rx="2" fill="#5c3818" />
                    
                    {/* Indoor Houseplant */}
                    <path d="M 680 320 C 660 300, 650 240, 680 230 C 710 240, 700 300, 680 320" fill="#2f5d3e" />
                    <path d="M 680 320 C 650 280, 630 250, 660 220 C 690 250, 690 280, 680 320" fill="#1e3f20" />
                    <rect x="665" y="310" width="30" height="40" rx="5" fill="#8d5b4c" />

                    {/* Perspective lines */}
                    <line x1="40" y1="110" x2="180" y2="100" stroke="rgba(255,255,255,0.1)" />
                    <line x1="760" y1="110" x2="620" y2="100" stroke="rgba(255,255,255,0.1)" />

                  </svg>

                  {/* Active Visualizer details floating overlay */}
                  <div className="absolute bottom-4 left-4 bg-black/80 p-3 rounded-lg text-xs space-y-1 backdrop-blur-md z-20 border border-white/10">
                    <p className="font-bold text-white uppercase tracking-wider">Live Configurator Settings:</p>
                    <p className="text-amber-400">Accent Wall: <span className="font-mono text-white">{wallColor}</span></p>
                    <p className="text-amber-400">Flooring: <span className="uppercase text-white">{floorType}</span></p>
                    <p className="text-amber-400">Cove Illumination: <span className="uppercase text-white">{lightingMode}</span></p>
                  </div>

                </div>

                <div className="mt-4 flex items-center justify-between">
                  <p className={`text-xs ${textMutedClass}`}>
                    *Note: This simulation represents a high-concept luxury living layout standard in Andal homes. Actual layouts are tailored meticulously on-site.
                  </p>
                  <button 
                    onClick={() => setActiveTab('contact')}
                    className={`px-4 py-2 rounded-lg text-xs font-bold uppercase transition-all ${buttonGold}`}
                  >
                    Discuss This Layout
                  </button>
                </div>

              </div>
            </div>

            {/* Design Controls sidebar */}
            <div className="lg:col-span-4 space-y-6">
              
              {/* Control Panel Wall Colors */}
              <div className={`p-6 rounded-3xl border ${cardBgClass} space-y-4 shadow-lg`}>
                <h4 className="font-extrabold text-base uppercase tracking-wider flex items-center gap-2">
                  <Paintbrush className="w-4 h-4 text-amber-500" /> Wall Paint Accents
                </h4>
                <p className={`text-xs ${textMutedClass}`}>Select a premium Asian Paints Royale-inspired shade for your focus wall:</p>
                
                <div className="grid grid-cols-2 gap-3">
                  {[
                    { color: '#d4af37', name: 'Royal Gold' },
                    { color: '#2c3e50', name: 'Heritage Blue' },
                    { color: '#2e4a3f', name: 'Moss Emerald' },
                    { color: '#e07a5f', name: 'Andal Terracotta' },
                    { color: '#ecebe4', name: 'Classic Silk Ivory' },
                    { color: '#3d3d3d', name: 'Modern Obsidian' }
                  ].map((shd, index) => (
                    <button
                      key={index}
                      onClick={() => setWallColor(shd.color)}
                      className={`flex items-center gap-2 p-2 rounded-lg border text-left transition-all ${
                        wallColor === shd.color 
                          ? 'border-amber-500 bg-amber-500/10' 
                          : isDark ? 'border-neutral-800 hover:bg-neutral-800' : 'border-stone-200 hover:bg-stone-100'
                      }`}
                    >
                      <span className="w-5 h-5 rounded-full block border shadow-sm" style={{ backgroundColor: shd.color }} />
                      <span className="text-[10px] font-bold uppercase tracking-tight">{shd.name}</span>
                    </button>
                  ))}
                </div>
              </div>

              {/* Control Panel Floor finishes */}
              <div className={`p-6 rounded-3xl border ${cardBgClass} space-y-4 shadow-lg`}>
                <h4 className="font-extrabold text-base uppercase tracking-wider flex items-center gap-2">
                  <Layers className="w-4 h-4 text-amber-500" /> Premium Flooring
                </h4>
                <div className="space-y-2">
                  {[
                    { id: 'marble', name: 'Italian White Statuario Marble', desc: 'Glossy, luxurious marble ideal for cool Indian summers.' },
                    { id: 'wooden', name: 'High-Density Teak Wood Laminate', desc: 'Warm, cozy luxury with moisture-resistant lock joint panels.' },
                    { id: 'tiles', name: 'Vitrified Matte Slate Tiles', desc: 'Sleek, low-maintenance urban look with minimum grout lines.' }
                  ].map((flr) => (
                    <button
                      key={flr.id}
                      onClick={() => setFloorType(flr.id)}
                      className={`w-full text-left p-3 rounded-xl border transition-all ${
                        floorType === flr.id 
                          ? 'border-amber-500 bg-amber-500/10' 
                          : isDark ? 'border-neutral-800 hover:bg-neutral-800' : 'border-stone-200 hover:bg-stone-100'
                      }`}
                    >
                      <p className="text-xs font-bold uppercase">{flr.name}</p>
                      <p className={`text-[10px] mt-1 ${textMutedClass}`}>{flr.desc}</p>
                    </button>
                  ))}
                </div>
              </div>

              {/* Control Panel Lighting settings */}
              <div className={`p-6 rounded-3xl border ${cardBgClass} space-y-4 shadow-lg`}>
                <h4 className="font-extrabold text-base uppercase tracking-wider flex items-center gap-2">
                  <Sparkles className="w-4 h-4 text-amber-500" /> False Ceiling Lighting
                </h4>
                <div className="grid grid-cols-3 gap-2">
                  {[
                    { id: 'warm', name: 'Sunset Warm', color: 'from-amber-400 to-yellow-600' },
                    { id: 'cool', name: 'Breeze Cool', color: 'from-sky-400 to-blue-600' },
                    { id: 'ambient', name: 'Neon Purple', color: 'from-purple-400 to-pink-600' }
                  ].map((lt) => (
                    <button
                      key={lt.id}
                      onClick={() => setLightingMode(lt.id)}
                      className={`p-2 rounded-lg border text-center transition-all ${
                        lightingMode === lt.id 
                          ? 'border-amber-500 bg-amber-500/10' 
                          : isDark ? 'border-neutral-800 hover:bg-neutral-800' : 'border-stone-200 hover:bg-stone-100'
                      }`}
                    >
                      <div className={`w-full h-3 rounded bg-gradient-to-r ${lt.color} mb-1`} />
                      <span className="text-[10px] font-bold uppercase block">{lt.name}</span>
                    </button>
                  ))}
                </div>
              </div>

            </div>

          </div>
        </section>
      )}

      {/* ==========================================
          TAB 3: AI DESIGNER PARTNER (GEMINI INTEGRATION)
          ========================================== */}
      {activeTab === 'ai-designer' && (
        <section className="py-12 max-w-5xl mx-auto px-4 sm:px-6 lg:px-8">
          <div className="text-center max-w-2xl mx-auto mb-10 space-y-3">
            <span className="inline-flex items-center gap-1.5 px-3 py-1 rounded-full text-xs font-bold bg-amber-500/15 text-amber-400 border border-amber-500/30">
              <Sparkles className="w-3.5 h-3.5" /> AI Designer Powered by Gemini 2.5
            </span>
            <h3 className="text-3xl font-black">Co-Create with Our AI Designer</h3>
            <p className={textMutedClass}>
              Instantly generate architectural layouts, materials selection lists, and accent ideas curated specifically for our regional climate in Andal.
            </p>
          </div>

          <div className={`p-8 rounded-3xl border ${cardBgClass} shadow-2xl space-y-6 relative overflow-hidden`}>
            
            <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
              
              {/* Form Input fields */}
              <div className="space-y-4">
                <div>
                  <label className="block text-xs font-bold uppercase tracking-wider text-amber-500 mb-2 font-bold">
                    1. Select Design Concept
                  </label>
                  <select
                    value={aiStyle}
                    onChange={(e) => setAiStyle(e.target.value)}
                    className={`w-full p-3 rounded-xl border font-semibold text-sm ${
                      isDark ? 'bg-neutral-900 border-neutral-800 text-white' : 'bg-stone-100 border-stone-200 text-stone-900'
                    }`}
                  >
                    <option value="Modern Minimalist">Modern Minimalist (Sleek & Light)</option>
                    <option value="Royal Bengali Heritage">Royal Bengali Heritage (Wood carvings & Terracotta accents)</option>
                    <option value="Industrial Chic">Industrial Chic (Bricks, Metal framework)</option>
                    <option value="Scandi Boho">Scandinavian Bohemian (Warm wool, Rattan & Indoor Plants)</option>
                    <option value="Luxurious Art Deco">Luxurious Art Deco (Golds, Deep Velvets & Geometrics)</option>
                  </select>
                </div>

                <div>
                  <label className="block text-xs font-bold uppercase tracking-wider text-amber-500 mb-2 font-bold">
                    2. Describe your room or custom needs
                  </label>
                  <textarea
                    rows={4}
                    value={aiPrompt}
                    onChange={(e) => setAiPrompt(e.target.value)}
                    placeholder="E.g., I have a 12x15 Master Bedroom with an attached balcony on the west wall. Suggest a false ceiling and bed position."
                    className={`w-full p-4 rounded-xl border text-sm font-medium ${
                      isDark ? 'bg-neutral-900 border-neutral-800 text-white placeholder-neutral-500' : 'bg-stone-100 border-stone-200 text-stone-900 placeholder-stone-400'
                    }`}
                  />
                </div>

                <button
                  onClick={generateAIDesign}
                  disabled={aiLoading}
                  className={`w-full py-4 rounded-xl font-bold tracking-wider uppercase transition-all shadow-lg flex items-center justify-center gap-2 ${buttonGold} ${
                    aiLoading ? 'opacity-50 cursor-not-allowed' : ''
                  }`}
                >
                  {aiLoading ? (
                    <>
                      <div className="w-5 h-5 border-2 border-neutral-950 border-t-transparent rounded-full animate-spin"></div>
                      Consulting Architect AI...
                    </>
                  ) : (
                    <>
                      Generate Custom Designer Guide <Sparkles className="w-4 h-4" />
                    </>
                  )}
                </button>
              </div>

              {/* Designer suggestions display */}
              <div className={`p-6 rounded-2xl border ${
                isDark ? 'bg-neutral-950 border-neutral-800' : 'bg-stone-50 border-stone-200'
              } flex flex-col justify-between min-h-[300px]`}>
                
                <div>
                  <div className="flex items-center justify-between pb-3 border-b border-neutral-800/10 mb-4">
                    <span className="text-xs uppercase tracking-widest font-bold text-amber-500 flex items-center gap-1.5">
                      <Sparkles className="w-3.5 h-3.5" /> Output Concept Board
                    </span>
                    <span className="text-[10px] font-mono px-2 py-0.5 rounded bg-amber-500/10 text-amber-400">
                      Free Preview
                    </span>
                  </div>

                  {aiLoading && (
                    <div className="space-y-4 animate-pulse pt-4">
                      <div className="h-4 bg-amber-500/20 rounded w-2/3"></div>
                      <div className="h-4 bg-amber-500/10 rounded w-5/6"></div>
                      <div className="h-4 bg-amber-500/10 rounded w-1/2"></div>
                      <div className="h-4 bg-amber-500/5 rounded w-4/5"></div>
                    </div>
                  )}

                  {!aiLoading && !aiResponse && !aiError && (
                    <div className="text-center py-12 space-y-3">
                      <div className="w-12 h-12 rounded-full bg-amber-500/10 text-amber-400 flex items-center justify-center mx-auto">
                        <Compass className="w-6 h-6" />
                      </div>
                      <p className="text-sm font-bold">Your Concept Board is Empty</p>
                      <p className={`text-xs max-w-xs mx-auto ${textMutedClass}`}>
                        Select your design theme and hit "Generate Custom Designer Guide" to receive personalized advice.
                      </p>
                    </div>
                  )}

                  {aiError && (
                    <div className="p-4 rounded-xl bg-red-500/10 border border-red-500/20 text-red-400 text-xs text-center space-y-2">
                      <p className="font-bold">Connection Issue</p>
                      <p>{aiError}</p>
                    </div>
                  )}

                  {!aiLoading && aiResponse && (
                    <div 
                      className={`text-xs sm:text-sm leading-relaxed max-h-[350px] overflow-y-auto space-y-3 pr-2 scrollbar-thin ${
                        isDark ? 'text-neutral-200' : 'text-stone-800'
                      }`}
                      dangerouslySetInnerHTML={{ __html: aiResponse }}
                    />
                  )}
                </div>

                {aiResponse && (
                  <div className="pt-4 mt-4 border-t border-neutral-800/10 flex items-center justify-between">
                    <p className={`text-[10px] ${textMutedClass}`}>Inspired by Shyam Interior Andal quality norms.</p>
                    <button
                      onClick={() => {
                        setBookingForm(prev => ({ 
                          ...prev, 
                          message: `Hi Shyam Interior, I generated an AI design concept for a "${aiStyle}" room based on my prompt: "${aiPrompt}". I'd like to implement this!` 
                        }));
                        setActiveTab('contact');
                      }}
                      className="text-xs font-bold text-amber-500 hover:underline flex items-center gap-1"
                    >
                      Use This Plan & Book <ChevronRight className="w-4 h-4" />
                    </button>
                  </div>
                )}

              </div>

            </div>

          </div>
        </section>
      )}

      {/* ==========================================
          TAB 4: COST ESTIMATOR
          ========================================== */}
      {activeTab === 'estimator' && (
        <section className="py-12 max-w-5xl mx-auto px-4 sm:px-6 lg:px-8">
          <div className="text-center max-w-2xl mx-auto mb-10 space-y-3">
            <h2 className="text-xs uppercase tracking-widest font-extrabold text-amber-500 font-bold">Budgeting Panel</h2>
            <h3 className="text-3xl font-black">Andal Interior Cost Estimator</h3>
            <p className={textMutedClass}>
              Slide to adjust area dimensions, choose luxury material tiers, and get a realistic estimate of your project.
            </p>
          </div>

          <div className="grid grid-cols-1 lg:grid-cols-12 gap-8 items-stretch">
            
            {/* Input Slider Controls */}
            <div className={`lg:col-span-7 p-8 rounded-3xl border ${cardBgClass} shadow-xl space-y-6`}>
              
              {/* Room type selection */}
              <div>
                <label className="block text-xs font-bold uppercase tracking-wider text-amber-500 mb-3 font-bold">
                  1. Choose Area To Design
                </label>
                <div className="grid grid-cols-3 gap-3">
                  {[
                    { id: 'living', label: 'Living Room' },
                    { id: 'bedroom', label: 'Master Bedroom' },
                    { id: 'kitchen', label: 'Modular Kitchen' }
                  ].map((rm) => (
                    <button
                      key={rm.id}
                      onClick={() => {
                        setRoomType(rm.id);
                        if (rm.id === 'kitchen') {
                          setAddons(prev => ({ ...prev, modularKitchen: true }));
                        }
                      }}
                      className={`p-3 rounded-xl border text-center transition-all ${
                        roomType === rm.id 
                          ? 'border-amber-500 bg-amber-500/10 font-bold' 
                          : isDark ? 'border-neutral-800 hover:bg-neutral-800 text-neutral-400' : 'border-stone-200 hover:bg-stone-100 text-stone-600'
                      }`}
                    >
                      <span className="text-xs tracking-tight block">{rm.label}</span>
                    </button>
                  ))}
                </div>
              </div>

              {/* Area Slider */}
              <div className="space-y-2">
                <div className="flex justify-between items-center text-xs uppercase tracking-wider font-bold">
                  <span className="text-amber-500 font-bold">2. Select Room Size</span>
                  <span className={accentGold}>{roomArea} Sq.Ft.</span>
                </div>
                <input
                  type="range"
                  min="80"
                  max="1000"
                  step="10"
                  value={roomArea}
                  onChange={(e) => setRoomArea(Number(e.target.value))}
                  className="w-full h-2 bg-neutral-800 rounded-lg appearance-none cursor-pointer accent-[#d4af37]"
                />
                <div className="flex justify-between text-[10px] text-neutral-500 font-mono">
                  <span>80 SQ.FT. (Small Cabin)</span>
                  <span>1000 SQ.FT. (Grand Hall)</span>
                </div>
              </div>

              {/* Material Grade Selection */}
              <div>
                <label className="block text-xs font-bold uppercase tracking-wider text-amber-500 mb-3 font-bold">
                  3. Select Material & Finish Class
                </label>
                <div className="space-y-3">
                  {[
                    { id: 'standard', name: 'Elite Classic (Value Tier)', rate: '₹120/sqft', desc: 'Standard ISI-marked ply, PVC seamless ceilings, premium tractor emulsion paint.' },
                    { id: 'premium', name: 'Shyam Premium (Highly Recommended)', rate: '₹220/sqft', desc: 'Waterproof BWR Ply, high-grade Saint-Gobain Gypsum ceilings, Asian Royale silk finishing, sleek LED profiles.' },
                    { id: 'ultra', name: 'Royal Opulence (Signature Tier)', rate: '₹380/sqft', desc: 'Authentic HDMR sheets, customized CNC Jali, multi-tier gypsum ceilings with smart lighting systems, premium wallpapers.' }
                  ].map((g) => (
                    <button
                      key={g.id}
                      onClick={() => setMaterialGrade(g.id)}
                      className={`w-full text-left p-3 rounded-xl border transition-all flex items-start justify-between gap-4 ${
                        materialGrade === g.id 
                          ? 'border-amber-500 bg-amber-500/10' 
                          : isDark ? 'border-neutral-800 hover:bg-neutral-800' : 'border-stone-200 hover:bg-stone-100'
                      }`}
                    >
                      <div>
                        <p className="text-xs font-bold uppercase">{g.name}</p>
                        <p className={`text-[10px] mt-1 ${textMutedClass}`}>{g.desc}</p>
                      </div>
                      <span className="text-xs font-mono font-bold text-amber-500 shrink-0">{g.rate}</span>
                    </button>
                  ))}
                </div>
              </div>

              {/* Addons Selection */}
              <div>
                <label className="block text-xs font-bold uppercase tracking-wider text-amber-500 mb-3 font-bold">
                  4. Design Add-ons
                </label>
                <div className="grid grid-cols-2 gap-3">
                  <button
                    onClick={() => setAddons(p => ({ ...p, falseCeiling: !p.falseCeiling }))}
                    className={`p-3 rounded-xl border text-left flex items-center justify-between transition-all ${
                      addons.falseCeiling ? 'border-amber-500 bg-amber-500/10' : 'border-neutral-800'
                    }`}
                  >
                    <span className="text-xs font-semibold">Designer False Ceiling</span>
                    {addons.falseCeiling ? <Check className="w-4 h-4 text-amber-500" /> : <div className="w-4 h-4 rounded-full border border-neutral-700" />}
                  </button>

                  {roomType === 'kitchen' && (
                    <button
                      onClick={() => setAddons(p => ({ ...p, modularKitchen: !p.modularKitchen }))}
                      className={`p-3 rounded-xl border text-left flex items-center justify-between transition-all ${
                        addons.modularKitchen ? 'border-amber-500 bg-amber-500/10' : 'border-neutral-800'
                      }`}
                    >
                      <span className="text-xs font-semibold">Full Cabinets Setup</span>
                      {addons.modularKitchen ? <Check className="w-4 h-4 text-amber-500" /> : <div className="w-4 h-4 rounded-full border border-neutral-700" />}
                    </button>
                  )}

                  <button
                    onClick={() => setAddons(p => ({ ...p, ambientLighting: !p.ambientLighting }))}
                    className={`p-3 rounded-xl border text-left flex items-center justify-between transition-all ${
                      addons.ambientLighting ? 'border-amber-500 bg-amber-500/10' : 'border-neutral-800'
                    }`}
                  >
                    <span className="text-xs font-semibold">Ambient LED Profiles</span>
                    {addons.ambientLighting ? <Check className="w-4 h-4 text-amber-500" /> : <div className="w-4 h-4 rounded-full border border-neutral-700" />}
                  </button>

                  <button
                    onClick={() => setAddons(p => ({ ...p, wallpapers: !p.wallpapers }))}
                    className={`p-3 rounded-xl border text-left flex items-center justify-between transition-all ${
                      addons.wallpapers ? 'border-amber-500 bg-amber-500/10' : 'border-neutral-800'
                    }`}
                  >
                    <span className="text-xs font-semibold">Metallic Accent Wallpaper</span>
                    {addons.wallpapers ? <Check className="w-4 h-4 text-amber-500" /> : <div className="w-4 h-4 rounded-full border border-neutral-700" />}
                  </button>
                </div>
              </div>

            </div>

            {/* Bill / Estimate breakdown display */}
            <div className="lg:col-span-5 flex flex-col justify-between">
              <div className={`p-8 rounded-3xl border ${cardBgClass} bg-gradient-to-tr ${isDark ? 'from-[#171512] to-[#1e1a14]' : 'from-white to-stone-50'} shadow-2xl flex-1 flex flex-col justify-between relative overflow-hidden`}>
                
                <div className="space-y-6">
                  <div className="pb-4 border-b border-neutral-800/10 flex justify-between items-center">
                    <div>
                      <h4 className="text-base font-extrabold uppercase tracking-wide">Investment Summary</h4>
                      <p className={`text-[10px] uppercase tracking-wider ${textMutedClass}`}>Budget Estimate Breakdown</p>
                    </div>
                    <span className="text-[10px] px-2 py-1 rounded bg-amber-500/10 text-amber-400 font-mono">
                      ANDAL REGION
                    </span>
                  </div>

                  <div className="space-y-3">
                    <div className="flex justify-between text-xs">
                      <span className={textMutedClass}>Base Wooden & Partitioning:</span>
                      <span className="font-mono font-bold">₹{costResult.baseCost.toLocaleString('en-IN')}</span>
                    </div>

                    {addons.falseCeiling && (
                      <div className="flex justify-between text-xs">
                        <span className={textMutedClass}>Plasterboard False Ceiling:</span>
                        <span className="font-mono font-bold text-green-500">+ ₹{(roomArea * 90).toLocaleString('en-IN')}</span>
                      </div>
                    )}

                    {addons.modularKitchen && roomType === 'kitchen' && (
                      <div className="flex justify-between text-xs">
                        <span className={textMutedClass}>Modular Kitchen Cabinet Works:</span>
                        <span className="font-mono font-bold text-green-500">+ ₹1,20,000</span>
                      </div>
                    )}

                    {addons.ambientLighting && (
                      <div className="flex justify-between text-xs">
                        <span className={textMutedClass}>LED Ambience & Cob Strips:</span>
                        <span className="font-mono font-bold text-green-500">+ ₹15,000</span>
                      </div>
                    )}

                    {addons.wallpapers && (
                      <div className="flex justify-between text-xs">
                        <span className={textMutedClass}>Designer Wallpaper Layers:</span>
                        <span className="font-mono font-bold text-green-500">+ ₹25,000</span>
                      </div>
                    )}

                    <div className="pt-4 border-t border-dashed border-neutral-800/15 flex justify-between items-end">
                      <div>
                        <span className="text-xs uppercase tracking-wider font-extrabold font-bold">Estimated Total</span>
                        <p className={`text-[10px] ${textMutedClass}`}>Turnkey implementation</p>
                      </div>
                      <span className="text-2xl sm:text-3xl font-black text-amber-500 font-mono">
                        ₹{costResult.total.toLocaleString('en-IN')}*
                      </span>
                    </div>
                  </div>

                  <div className={`p-4 rounded-xl text-xs space-y-1.5 ${
                    isDark ? 'bg-neutral-900/60' : 'bg-stone-100'
                  }`}>
                    <p className="font-bold flex items-center gap-1">
                      <Check className="w-3.5 h-3.5 text-green-500" /> What's Included?
                    </p>
                    <ul className={`list-disc pl-4 space-y-1 text-[11px] ${textMutedClass}`}>
                      <li>Detailed 2D & 3D floor plan architectural preview</li>
                      <li>Highly durable premium BWR Plywood frames & anti-termite treatment</li>
                      <li>Hettich or Hafele designer soft-close hardware accessories</li>
                      <li>Complete post-completion cleaning & dust scrubbing</li>
                    </ul>
                  </div>
                </div>

                <div className="pt-6 mt-6 border-t border-neutral-800/10 space-y-4">
                  <p className={`text-[10px] ${textMutedClass} leading-relaxed`}>
                    *Disclaimer: Estimations are approximate and based on typical Andal residential specifications. Exact prices vary with on-site measurement and materials chosen from our catalogs.
                  </p>
                  
                  <button
                    onClick={() => {
                      setBookingForm(prev => ({
                        ...prev,
                        message: `Hi Shyam Interior, I got a budget estimate of ₹${costResult.total.toLocaleString('en-IN')} for my ${roomArea} sq.ft. ${roomType} space using ${materialGrade} materials. I'd like to book an executive consultation.`
                      }));
                      setActiveTab('contact');
                    }}
                    className={`w-full py-4 rounded-xl text-sm font-bold tracking-wider uppercase transition-all shadow-lg flex items-center justify-center gap-2 ${buttonGold}`}
                  >
                    Lock Estimate & Book Consultation <ArrowRight className="w-4 h-4" />
                  </button>
                </div>

              </div>
            </div>

          </div>
        </section>
      )}

      {/* ==========================================
          TAB 5: PORTFOLIO SHOWCASE
          ========================================== */}
      {activeTab === 'portfolio' && (
        <section className="py-12 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div className="text-center max-w-2xl mx-auto mb-10 space-y-3">
            <h2 className="text-xs uppercase tracking-widest font-extrabold text-amber-500">Completed Ventures</h2>
            <h3 className="text-3xl font-black">Our Premium Portfolio</h3>
            <p className={textMutedClass}>
              Explore our real completed projects around Andal, Raniganj, and Durgapur. High-fidelity craftsmanship delivered to happy homeowners.
            </p>

            {/* Category filter pills */}
            <div className="flex flex-wrap gap-2 justify-center pt-4">
              {[
                { id: 'all', label: 'All Projects' },
                { id: 'living', label: 'Living Rooms' },
                { id: 'kitchen', label: 'Kitchens' },
                { id: 'bedroom', label: 'Bedrooms' },
                { id: 'ceiling', label: 'False Ceilings' }
              ].map((pill) => (
                <button
                  key={pill.id}
                  onClick={() => setGalleryFilter(pill.id)}
                  className={`px-4 py-2 rounded-full text-xs font-bold tracking-wider uppercase transition-all ${
                    galleryFilter === pill.id 
                      ? 'bg-[#d4af37] text-neutral-950 font-extrabold' 
                      : isDark ? 'bg-neutral-900 text-neutral-400 hover:text-white' : 'bg-stone-200 text-stone-600 hover:text-stone-900'
                  }`}
                >
                  {pill.label}
                </button>
              ))}
            </div>
          </div>

          <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
            {galleryItems
              .filter(item => galleryFilter === 'all' || item.category === galleryFilter)
              .map((item, index) => (
                <div 
                  key={index}
                  className={`group rounded-2xl overflow-hidden border ${cardBgClass} hover:border-[#d4af37]/50 shadow-xl transition-all duration-300`}
                >
                  {/* Image Placeholder Frame (Beautiful SVGs inside) */}
                  <div className={`relative h-60 ${item.imageColor} flex items-center justify-center p-6 text-center overflow-hidden`}>
                    
                    {/* Simulated architectural grid lines */}
                    <div className="absolute inset-0 bg-[linear-gradient(rgba(255,255,255,0.02)_1px,transparent_1px),linear-gradient(90deg,rgba(255,255,255,0.02)_1px,transparent_1px)] bg-[size:20px_20px]"></div>

                    {/* Architectural outline blueprint icon */}
                    <div className="z-10 text-white/40 group-hover:scale-110 transition-transform duration-500">
                      <Compass className="w-16 h-16 mx-auto stroke-1" />
                    </div>

                    <div className="absolute bottom-4 left-4 text-left z-10">
                      <span className="text-[10px] font-mono font-bold bg-black/60 text-amber-400 px-2.5 py-1 rounded backdrop-blur-sm">
                        {item.location}
                      </span>
                    </div>
                  </div>

                  {/* Portfolio Details */}
                  <div className="p-6 space-y-4">
                    <div className="flex justify-between items-start">
                      <h4 className="text-lg font-bold group-hover:text-amber-500 transition-colors">{item.title}</h4>
                      <span className="text-[10px] font-bold uppercase text-[#d4af37] bg-[#d4af37]/10 px-2 py-0.5 rounded">
                        {item.category}
                      </span>
                    </div>

                    <div className="flex flex-wrap gap-2">
                      {item.tags.map((tag, tIdx) => (
                        <span 
                          key={tIdx} 
                          className={`text-[10px] font-semibold px-2 py-0.5 rounded ${
                            isDark ? 'bg-neutral-900 text-neutral-400' : 'bg-stone-100 text-stone-600'
                          }`}
                        >
                          #{tag}
                        </span>
                      ))}
                    </div>

                    <button
                      onClick={() => {
                        setBookingForm(prev => ({
                          ...prev,
                          message: `Hi, I saw your project "${item.title}" in your portfolio. I'm looking to do something similar in Andal!`
                        }));
                        setActiveTab('contact');
                      }}
                      className="w-full py-2.5 rounded-xl text-xs font-bold uppercase transition-all bg-neutral-800 text-white hover:bg-neutral-700/80 hover:text-amber-400 flex items-center justify-center gap-2"
                    >
                      Enquire for similar layout <ChevronRight className="w-4 h-4" />
                    </button>
                  </div>
                </div>
              ))}
          </div>
        </section>
      )}

      {/* ==========================================
          TAB 6: CONTACT & BOOKING ENGINE
          ========================================== */}
      {activeTab === 'contact' && (
        <section className="py-12 max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
          <div className="text-center max-w-2xl mx-auto mb-10 space-y-3">
            <h2 className="text-xs uppercase tracking-widest font-extrabold text-amber-500">Get In Touch</h2>
            <h3 className="text-3xl font-black">Schedule Executive Consultation</h3>
            <p className={textMutedClass}>
              Ready to design your absolute dream space? Book a premium survey or step by our Andal consultation hub.
            </p>
          </div>

          <div className="grid grid-cols-1 lg:grid-cols-12 gap-8 items-stretch">
            
            {/* Contact Details Panel */}
            <div className="lg:col-span-5 flex flex-col justify-between space-y-6">
              
              <div className={`p-8 rounded-3xl border ${cardBgClass} shadow-xl space-y-6 flex-1 flex flex-col justify-between`}>
                <div className="space-y-6">
                  <span className="text-xs font-bold uppercase text-amber-500 tracking-widest">Office Location</span>
                  <h4 className="text-2xl font-black">Shyam Interior Andal</h4>
                  
                  <div className="space-y-4">
                    <div className="flex items-start gap-3">
                      <MapPin className="w-5 h-5 text-amber-500 shrink-0 mt-1" />
                      <p className={`text-xs sm:text-sm leading-relaxed ${textMutedClass}`}>
                        <strong>Studio Address:</strong> Near Andal Junction & Kajora Bypass, Andal, West Bengal 713321
                      </p>
                    </div>

                    <div className="flex items-start gap-3">
                      <Phone className="w-5 h-5 text-amber-500 shrink-0 mt-1" />
                      <p className={`text-xs sm:text-sm leading-relaxed ${textMutedClass}`}>
                        <strong>Direct Mobile:</strong> +91 98321 00000 / +91 82500 00000 <br />
                        <span className="text-[10px] text-green-500 font-bold">● Live on WhatsApp for Instant Catalogs</span>
                      </p>
                    </div>

                    <div className="flex items-start gap-3">
                      <Clock className="w-5 h-5 text-amber-500 shrink-0 mt-1" />
                      <p className={`text-xs sm:text-sm leading-relaxed ${textMutedClass}`}>
                        <strong>Operating Hours:</strong> Monday – Sunday (10:00 AM – 8:00 PM)
                      </p>
                    </div>
                  </div>
                </div>

                <div className="pt-6 border-t border-neutral-800/10 space-y-2">
                  <p className="text-xs font-bold uppercase text-amber-500 font-bold">Service Coverage:</p>
                  <p className={`text-xs ${textMutedClass}`}>
                    Andal, Kajora, Durgapur, Raniganj, Asansol, and nearby West Bengal townships.
                  </p>
                </div>
              </div>

              {/* Map mockup */}
              <div className={`p-6 rounded-3xl border ${cardBgClass} bg-stone-900 overflow-hidden relative aspect-[16/9] shadow-lg flex items-center justify-center text-center`}>
                {/* Abstract graphic representing location pins */}
                <div className="absolute inset-0 bg-[radial-gradient(ellipse_at_center,rgba(212,175,55,0.08),transparent)]"></div>
                <div className="absolute inset-0 bg-[linear-gradient(rgba(255,255,255,0.01)_1px,transparent_1px),linear-gradient(90deg,rgba(255,255,255,0.01)_1px,transparent_1px)] bg-[size:15px_15px]"></div>
                
                <div className="z-10 space-y-2">
                  <MapPin className="w-10 h-10 text-amber-500 mx-auto animate-bounce" />
                  <p className="text-xs font-bold text-white uppercase tracking-wider">Andal Junction Bypass Hub</p>
                  <p className="text-[10px] text-neutral-400">Centrally located for rapid team dispatch and direct wood selection</p>
                </div>
              </div>

            </div>

            {/* Live Appointment Booking Form */}
            <div className={`lg:col-span-7 p-8 rounded-3xl border ${cardBgClass} shadow-xl relative overflow-hidden`}>
              
              <div className="pb-4 mb-6 border-b border-neutral-800/10">
                <h4 className="text-lg font-bold">Executive Booking Portal</h4>
                <p className={`text-xs ${textMutedClass}`}>Submit details below and a senior interior supervisor will contact you within 4 hours.</p>
              </div>

              {formSubmitted ? (
                <div className="text-center py-16 space-y-4">
                  <div className="w-16 h-16 rounded-full bg-green-500/10 text-green-500 flex items-center justify-center mx-auto">
                    <Check className="w-10 h-10" />
                  </div>
                  <h4 className="text-xl font-bold">Inquiry Transmitted Successfully</h4>
                  <p className={`text-xs max-w-sm mx-auto ${textMutedClass}`}>
                    Thank you! Your architectural profile is locked. Our senior executive from the Andal office will call you shortly to confirm dates.
                  </p>
                  <p className="text-xs text-amber-500 font-bold font-bold font-bold font-bold">In-Memory Booking Confirmed</p>
                </div>
              ) : (
                <form onSubmit={handleFormSubmit} className="space-y-4">
                  
                  <div className="grid grid-cols-1 sm:grid-cols-2 gap-4">
                    <div>
                      <label className="block text-xs font-bold uppercase tracking-wider text-amber-500 mb-1.5 font-bold">
                        Your Full Name *
                      </label>
                      <input
                        type="text"
                        required
                        value={bookingForm.name}
                        onChange={(e) => setBookingForm(prev => ({ ...prev, name: e.target.value }))}
                        placeholder="John Doe"
                        className={`w-full p-3 rounded-xl border text-xs font-medium ${
                          isDark ? 'bg-neutral-900 border-neutral-800 text-white' : 'bg-stone-100 border-stone-200 text-stone-900'
                        }`}
                      />
                    </div>

                    <div>
                      <label className="block text-xs font-bold uppercase tracking-wider text-amber-500 mb-1.5 font-bold">
                        WhatsApp Contact Number *
                      </label>
                      <input
                        type="tel"
                        required
                        value={bookingForm.phone}
                        onChange={(e) => setBookingForm(prev => ({ ...prev, phone: e.target.value }))}
                        placeholder="+91 XXXXX XXXXX"
                        className={`w-full p-3 rounded-xl border text-xs font-medium ${
                          isDark ? 'bg-neutral-900 border-neutral-800 text-white' : 'bg-stone-100 border-stone-200 text-stone-900'
                        }`}
                      />
                    </div>
                  </div>

                  <div className="grid grid-cols-1 sm:grid-cols-2 gap-4">
                    <div>
                      <label className="block text-xs font-bold uppercase tracking-wider text-amber-500 mb-1.5 font-bold">
                        Email Address
                      </label>
                      <input
                        type="email"
                        value={bookingForm.email}
                        onChange={(e) => setBookingForm(prev => ({ ...prev, email: e.target.value }))}
                        placeholder="john@example.com"
                        className={`w-full p-3 rounded-xl border text-xs font-medium ${
                          isDark ? 'bg-neutral-900 border-neutral-800 text-white' : 'bg-stone-100 border-stone-200 text-stone-900'
                        }`}
                      />
                    </div>

                    <div>
                      <label className="block text-xs font-bold uppercase tracking-wider text-amber-500 mb-1.5 font-bold">
                        Service Required
                      </label>
                      <select
                        value={bookingForm.service}
                        onChange={(e) => setBookingForm(prev => ({ ...prev, service: e.target.value }))}
                        className={`w-full p-3 rounded-xl border text-xs font-medium ${
                          isDark ? 'bg-neutral-900 border-neutral-800 text-white' : 'bg-stone-100 border-stone-200 text-stone-900'
                        }`}
                      >
                        <option value="Residential Design">Premium Residential Design</option>
                        <option value="False Ceiling Work">Architectural False Ceiling</option>
                        <option value="Modular Kitchen">Smart Modular Kitchen</option>
                        <option value="Commercial Design">Corporate/Showroom Design</option>
                      </select>
                    </div>
                  </div>

                  <div className="grid grid-cols-1 sm:grid-cols-2 gap-4">
                    <div>
                      <label className="block text-xs font-bold uppercase tracking-wider text-amber-500 mb-1.5 font-bold">
                        Location in Andal/Asansol
                      </label>
                      <input
                        type="text"
                        value={bookingForm.location}
                        onChange={(e) => setBookingForm(prev => ({ ...prev, location: e.target.value }))}
                        placeholder="E.g. Andal Airport Area, Kajora"
                        className={`w-full p-3 rounded-xl border text-xs font-medium ${
                          isDark ? 'bg-neutral-900 border-neutral-800 text-white' : 'bg-stone-100 border-stone-200 text-stone-900'
                        }`}
                      />
                    </div>

                    <div>
                      <label className="block text-xs font-bold uppercase tracking-wider text-amber-500 mb-1.5 font-bold">
                        Preferred Survey Date
                      </label>
                      <input
                        type="date"
                        value={bookingForm.date}
                        onChange={(e) => setBookingForm(prev => ({ ...prev, date: e.target.value }))}
                        className={`w-full p-3 rounded-xl border text-xs font-medium ${
                          isDark ? 'bg-neutral-900 border-neutral-800 text-white' : 'bg-stone-100 border-stone-200 text-stone-900'
                        }`}
                      />
                    </div>
                  </div>

                  <div>
                    <label className="block text-xs font-bold uppercase tracking-wider text-amber-500 mb-1.5 font-bold font-bold font-bold">
                      Describe your dream design layout
                    </label>
                    <textarea
                      rows={4}
                      value={bookingForm.message}
                      onChange={(e) => setBookingForm(prev => ({ ...prev, message: e.target.value }))}
                      placeholder="Share structural requirements or select ideas from the visualizer..."
                      className={`w-full p-3 rounded-xl border text-xs font-medium ${
                        isDark ? 'bg-neutral-900 border-neutral-800 text-white' : 'bg-stone-100 border-stone-200 text-stone-900'
                      }`}
                    />
                  </div>

                  <button
                    type="submit"
                    className={`w-full py-4 rounded-xl text-sm font-bold tracking-wider uppercase transition-all shadow-lg flex items-center justify-center gap-2 ${buttonGold}`}
                  >
                    Submit Booking Request <Send className="w-4 h-4" />
                  </button>

                </form>
              )}

            </div>

          </div>
        </section>
      )}

      {/* ==========================================
          FOOTER
          ========================================== */}
      <footer className={`py-12 mt-20 border-t ${
        isDark ? 'bg-[#0b0a08] border-neutral-800/60' : 'bg-stone-100 border-stone-200'
      }`}>
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 grid grid-cols-1 md:grid-cols-4 gap-8">
          
          <div className="space-y-4">
            <span className="text-lg font-black tracking-widest block uppercase">
              SHYAM <span className={accentGold}>INTERIOR</span>
            </span>
            <p className={`text-xs leading-relaxed ${textMutedClass}`}>
              The standard of bespoke, premium design transformations in Andal, West Bengal. Elevating lifestyles through curated plasterboard false ceilings, high-end modular kitchens, and smart spatial planning.
            </p>
          </div>

          <div>
            <h4 className="text-xs uppercase tracking-widest font-extrabold text-amber-500 mb-4 font-bold">Quick Navigation</h4>
            <ul className="space-y-2 text-xs">
              {[
                { id: 'home', label: 'Home Portfolio' },
                { id: 'visualizer', label: 'Color Visualizer' },
                { id: 'ai-designer', label: 'AI Design Companion' },
                { id: 'estimator', label: 'Budget Estimator' }
              ].map((lnk) => (
                <li key={lnk.id}>
                  <button onClick={() => setActiveTab(lnk.id)} className={`hover:text-amber-500 transition-colors ${textMutedClass}`}>
                    {lnk.label}
                  </button>
                </li>
              ))}
            </ul>
          </div>

          <div>
            <h4 className="text-xs uppercase tracking-widest font-extrabold text-[#d4af37] mb-4 font-bold">Specializations</h4>
            <ul className={`space-y-2 text-xs ${textMutedClass}`}>
              <li>Gypsum & PVC False Ceilings</li>
              <li>Waterproof BWR Plywood Modular Kitchens</li>
              <li>Upholstered Accent Walls</li>
              <li>Durable Living & Master Bedroom Styling</li>
            </ul>
          </div>

          <div>
            <h4 className="text-xs uppercase tracking-widest font-extrabold text-amber-500 mb-4 font-bold">Newsletter Signup</h4>
            <p className={`text-xs mb-3 ${textMutedClass}`}>Receive premium interior catalog PDF editions right to your inbox.</p>
            <div className="flex gap-2">
              <input 
                type="email" 
                placeholder="Enter email" 
                className={`p-2.5 rounded-lg text-xs w-full ${
                  isDark ? 'bg-neutral-900 border-neutral-800 text-white' : 'bg-stone-100 border-stone-200 text-stone-900'
                }`} 
              />
              <button className={`p-2.5 rounded-lg ${buttonGold}`}>
                <ChevronRight className="w-4 h-4 text-neutral-950" />
              </button>
            </div>
          </div>

        </div>

        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 mt-12 pt-8 border-t border-neutral-800/10 text-center flex flex-col sm:flex-row justify-between items-center gap-4">
          <p className={`text-[10px] ${textMutedClass}`}>
            © {new Date().getFullYear()} Shyam Interior, Andal. All rights reserved. Designed to luxurious standards.
          </p>
          <div className="flex gap-4 text-[10px]">
            <a href="#" className={`hover:text-amber-500 ${textMutedClass}`}>Privacy Policy</a>
            <a href="#" className={`hover:text-amber-500 ${textMutedClass}`}>Terms of Service</a>
          </div>
        </div>
      </footer>

    </div>
  );
}

```